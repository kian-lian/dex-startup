# Changesets

This project uses [changesets](https://github.com/changesets/changesets) for versioning and changelog generation.

Run `pnpm changeset` to add a new changeset when you make a notable change.
